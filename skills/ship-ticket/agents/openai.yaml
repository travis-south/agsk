interface:
  display_name: "Ship Ticket"
  short_description: "Ship a ticket or prompt through reviews and a draft PR"
  default_prompt: "Use $ship-ticket to implement, review, and open a draft pull request for this ticket or requirement."
policy:
  allow_implicit_invocation: true
